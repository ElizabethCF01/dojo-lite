export type ClassItem = {
  id: string;
  name: string;
  teacherId: string;
  joinCode: string;
  createdAt: string;
};

export type RosterStudent = {
  id: string;
  name: string;
  email: string;
  enrolledAt: string;
};
